var React = require('react');

var Header = React.createClass(
  {
    render: function () {
      return (
        <div>
          <h1>Markdown Previewer</h1>
          <h3>::hear me roar::</h3>
        </div>)
    }
  }
);

module.exports = Header;
